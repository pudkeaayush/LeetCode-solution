The idea is to use recursion out here. We use a StringBuilder to append the value and '->'. We do this
recursively for the left part and right part. Detecting a leaf path is by the following condition :
root.left == null && root.right == null. We return when root is null.
