The point here is to use two pointers. Pointer i will start from the front of the string and pointer j will start from the end.
We keep comparing the characters at i and j for equality if they are characters , else we return false. Note that we skip if the
characters are not digits or alphabets.
