The idea here is to do a DFS traversal everytime we encounter 1. While doing the DFS set 1 to 0 so
that we do not consider it again. After every DFS traversal increment the count.
Return the count as the answer.
